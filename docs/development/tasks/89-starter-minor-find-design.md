# Task 89 - Design the first real minor finds

Current content revision: [110](110-bottle-physics-and-recognition.md) retains only three enlarged bottles, adds 60% reveal/physics and removes can/brick assets. Original delivery counts below are historical; use the current batch/catalog for implementation.

Type: design/research. Status: `done`. The user delegated selection, quantities, prices and sizes from twelve liked categories and explicitly requested model creation. [Completion](../completed/89-starter-minor-find-design.md). Model authoring is the bounded follow-up `108`; Trial integration is `109`; final production acceptance remains `09`.

Feature: [discovery content](../../features/backlog/discovery-content.md). Preserve [collection rules](../../features/backlog/discovery-collection.md). Research: [progression and discovery](../../research/player-review-findings.md#progression-and-discovery). Delivery: [09](09-starter-discoveries.md); later roster: [40](40-discovery-roster.md).

## Scope

- Design the first production batch of minor/ordinary finds to replace the current development forms. Prepare a concrete named roster for review before asking the user to choose individual objects.
- Selected through the user's delegated choice: **Glass Bottle, Food/Drink Can and Brick**, with seven visual variants and 72 placed instances. The [owning batch specification](../../features/backlog/starter-find-batch.md) records exact dimensions, counts, prices, IDs, material/recognition direction and migration mapping. The other liked categories remain later roster input. A toy car stays above common; site design remains `58`.
- Specify the exact number of distinct item types, any variants, the total placed population and per-type quantities for this first batch. Distinguish types from repeated instances; the current 96-object field and full-game 20–30 ordinary-type target do not automatically determine this batch's count.
- Give each item a stable content ID, display name, dimensions, silhouette, materials/colors and identifying details, size/slot classification, initial sale value, placement/orientation/clearance and burial/depth tendencies. All three are minor/common and **detector-ineligible without exception**, irrespective of size, metal or clusters. State partial-exposure recognition requirements; do not depend on a signal to make the items noticeable.
- Apply the [Steam audit's opening/identity test](../../research/steam-review-audit/discovery-and-payoff.md) at the common tier: recognizable partial shapes, satisfying repeated uncovering/collection and useful baseline income. Do not demand a memorable special moment from every ordinary pickup or put a toy car into this batch to force novelty. State what `09`/`15` should observe; later content owns higher-tier surprise and full-opening variety.
- Preserve held pickup, current 40% exposure plus actual visibility/range, ordinary inventory/sale behavior and durable identities. Describe compatibility when replacing development content in existing saves; do not silently reset or duplicate finds.
- Original Blender MCP model creation for the selected subset is now explicitly requested and owned by [108](108-starter-find-models.md), with files/source/license/removal in the ledger. `105` retains final style review; `109` has explicit trial integration approval, with later better models expected. `09` consumes the [replacement guide](../replacing-find-models.md) for final content. Do not treat earlier documentation-only limits as cancelling the user's later model request.
- This task owns the starter item selection formerly bundled into `57`. Presentation `57` consumes the accepted set; `40` counts it once and plans the remaining full-game roster. Large/distinctive finds, passive rewards and the final object remain with their existing tasks.

## Acceptance and review

- The owning feature contains one reviewed item table with exact counts and complete per-item specifications, a coherent visual/theme rationale, replacement/save plan and an explicit handoff to `09`.
- Resolve the batch theme, exact item set/counts and significant specification choices with the user using the concrete proposal. Record selected versus deferred options; numerical estimates remain clearly labeled until tested.
- Update `09`, `57` and `40` to consume the delegated selection. Design completion does not mark gameplay implemented; separately requested original asset production is delivered by `108`.
