# Task 148 - Validate the excavation and discovery slice before bulk content

Type: validation; user/player review. Status: `planned`. Prerequisites: decision `120`, approved first batch `42`, detector `10`, display `51`, material revisit `39`, production feedback `102`, reviewed progression `56`/`120`, and only a subsequently approved `147` scope if one is selected. The current admin experiment is not a prerequisite or accepted substitute. No prototype primitives or unapproved assets may stand in for these cases.

Features: [shovel](../../features/progression.md), [discoveries](../../features/discoveries.md), [display](../../features/discoveries.md). [Hypotheses](../../research/excavation-distinctiveness.md), [verdict workflow](../playtesting.md).

## Required comparison

- Use the restored normal game as baseline. Future guns are intended, with implementation deferred; `147` trial art/cuts may be a separately labelled research comparison only. Shave liking does not validate the current model, other modes or full progression. Consume `155`’s reviewed early-encounter policy and measure first recognition/first useful purchase on actual routes; its ten-minute goal remains a hypothesis. Audio direction is selected (ambient nature plus digging/action feedback); specific sounds still need approval.

- Use one reviewable MainGame build and five recognizable objects briefed in `40`/delivered by `42`, counting retained content once. Include handheld, appliance and occasional vehicle-scale cases under the `96`/`97` handling decision. No forty-metre expansion or vehicle in an ordinary slot.
- Observe three reward-free minutes, a 15-minute signal-led/free route and a 20–30-minute discovery expedition. Compare early/major-upgrade kit, an old hard formation, actual return/sale and voluntary browsing of the first few personal records.
- Ask whether locating and uncovering one appliance is entertaining before expanding to dozens of types. Record first recognition, voluntary continuation, mode choice, lateral intent, remembered objects and purchase explanation without stats.
- Use the linked numerical thresholds as hypotheses. Start with a small target-player sample (the report suggests five), record individual counts/comments and prepared versus naturally earned kit. Do not mark current-player sheets OK or claim population-level proof from this sample.

## Acceptance and handoff

- Record build/seed/equipment, each delivered case, findings and explicit keep/revise/omit decisions with the user. Missing required content is NOT READY; silence is not a pass.
- Route failures to `09`/`10`/`11`/`147`, `40`/`42`, `39` or `60`/`51`; retest affected cases. A new product decision returns to its design owner.
- Broad `41`/`43`/`44` production starts only after this gate passes or the user explicitly changes that priority. First-batch objects remain part of their eventual roster, never throwaway duplicates.
- Deliver the reviewed Windows build and a concise result. `37` still owns full-run boredom/economy; `54` hardware qualification; `153` positioning. Do not recruit or message testers without authorization.
