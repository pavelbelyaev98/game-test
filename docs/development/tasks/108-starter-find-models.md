# Task 108 - Author the first common-find models in Blender

Current content revision: [110](110-bottle-physics-and-recognition.md) retains only three enlarged bottles, adds 60% reveal/physics and removes can/brick assets. Original delivery counts below are historical; use the current batch/catalog for implementation.

Type: implementation; asset authoring only, explicitly selected by the user. Status: `done` for the requested model revision. [Delivery record](../completed/108-starter-find-models.md). `89`'s delegated selection is recorded in the [starter batch](../../features/backlog/starter-find-batch.md). The user approved `109` trial integration; final visual acceptance remains `09`/`105`.

## Scope and authorization

- The user supplied twelve liked categories and explicitly instructed selection of quantities/prices/sizes and creation of Blender MCP models in the preferred texture-reference style. This authorizes the chosen three-category/seven-model original source/export/preview batch, within the listed categories. The prior documentation-only restriction does not prevent this newly requested model work.
- Create three glass bottle shapes, intact/crushed cans and whole/chipped bricks at the recorded real-world scale. Preserve current Blender work in a separate scene; keep exact ownership under `art/starter-finds/`. Use original Blender-authored meshes/materials, with editable `.blend` source and reproducible recipe; no third-party artwork, music, extra props or world changes.
- Deliver UV-mapped FBX meshes, shared category texture atlases for base color/tangent normal/material response, a content/dimension/value/count manifest, coherent preview renders and a concise source/export guide. Keep preview staging out of exported game meshes.
- Record the specific batch, permission, license, files and removal in the asset ledger before using it. Creating these requested models is not acceptance of the whole game's final style, which remains `105`.
- User revision: the first pass looks bland. Add richer material-specific wear, torn labels and barely readable Bulgarian packaging/stamps to these same seven models. Use Blender's built-in text outlines and original label artwork; no new downloaded font/texture dependency. Preserve quantities, scale, category/value and silent-detector rules. The user approved the revised batch for `109` trial integration and expects later replacement models.

## Acceptance

- Inspect the actual Blender result and rendered previews, refine visible defects and show all seven models clearly. All three silhouettes/material groups must read coherently in the painterly reference direction; no untextured substitute forms.
- Verify names, dimensions, unit scale, applied export transforms, UV bounds/packing, finite vertices, surface normals, triangle/material budgets, external texture paths and export file presence. Reopen the retained source or reimport the exports through Blender MCP for a meaningful delivery check.
- `89`/the batch feature own exact initial counts/prices and migration requirements; `09` consumes these artifacts rather than commissioning duplicates. Record geometry/presentation limitations that still require actual burial and collection testing in Unity.
- Deliver original sources, exports and previews with a concise completion record. This asset-only task does not mark `09`, `105` or the game's production presentation complete and does not require replacing the current gameplay build.

Evidence: [source/export guide and concrete integration proposal](../../../art/starter-finds/README.md), overview (temporary capture cleaned), [manifest](../../../art/starter-finds/manifest.json) and [validation](../../../art/starter-finds/validation.json). Seven source meshes and FBX reimports pass geometry/UV/dimension checks; nine 2048px maps, 9,508 total triangles. Revised baked renders show richer material-specific wear, torn Bulgarian labels/stamps and a recessed can lid/pull tab; editable text/curve artwork is retained. Trial integration/evidence is owned by `109`; final style and recognition acceptance remain `105`/`09`.
