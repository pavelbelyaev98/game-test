# Task 152 - Evaluate optional automatic camp evolution

Type: design/research. Status: `planned`, deferred optional polish. Prerequisites: `148`, `51`, reviewed site sections `137`/`130`; no release dependency and no bypass of the reservoir asset verdict.

Feature: [optional systems](../../features/release.md). Context: [feedback](../../research/excavation-distinctiveness.md). `11` owns tool attachment changes and `51` discovery records; do not duplicate those systems.

## Decision artifact and questions

- Compare the current camp plus tool/archive changes against four proposed automatic surface states tied to owned milestones. Show what each state communicates from normal arrival views and why existing feedback is insufficient.
- Keep station access, accepted grass/ground, the selected reservoir premise and short visits. No placement/decorating UI, crafting, upkeep, base-building progression or scenery added just to fill space.
- List conditional assets, milestone/save/reset/Continue behaviour and independent rollback ownership. A state description is not asset approval; prepare no new models/audio before a scoped request.
- Propose a recognition test using reordered state views, accounting for changes already supplied by `11`/`51`. Review whether the added visibility justifies art and maintenance cost.

Done when include/defer/omit, concrete state brief and rationale live in the optional feature. Create a separate numbered implementation task only for an included decision; do not hold core gameplay or release for this polish.
