# Task 09 - Production starter finds and collection presentation

Type: implementation. Status: `planned`. Prerequisites: 05, 06, 07, 08, 57, 89, 108, 109, 110.

Feature: [discovery collection](../../features/backlog/discovery-collection.md). [Queue](../tasks.md). [Research basis](../../research/player-review-findings.md).

`89` selected the [starter batch](../../features/backlog/starter-find-batch.md); `108` authored its initial models and `109` integrates them as **replaceable trial art** at the user's explicit request. `110` selects only three enlarged bottles, removes cans/bricks, requires 60% exposure and implements physical release. `114` adds one Rock with three replaceable appearances, random orientation and the same support/pickup baseline. Better models are expected later. Final production art/style and recognition presentation remain this task, `105` and `57`; trial integration does not complete them.

Use the [replacement/reference guide](../replacing-find-models.md) and [editable catalog](../../../art/starter-finds/catalog.json). Reuse the implemented field, stable IDs, real-mesh samples, exact quotas and legacy migration. Model identity is separate from asset files; do not freeze the current three bottle meshes as final or reimplement the integration for replacements. Bulgarian writing is optional surface flavor, never required reading.

## Task contract

- After `08` and production acceptance in `05`-`07`, review/replace the trial assets against `105`/`57` and run the catalog sync. Retain `109`'s stable IDs, deterministic exact quotas, centred geometry/convex colliders/samples, support-release physics and compatible saves. Update sources/specs/ledger when models change, verify replacements through partial uncovering, and obtain final art/recognition acceptance. Richer weighted/cluster generation stays with `45`.
- Acceptance: finds require their authored exposure threshold and actual visibility. Holding/toggling Dig collects the directly aimed eligible find exactly once without another press (`112`); the scoop area and terrain/physics events cannot collect off-aim finds; full inventory leaves ordinary loot intact. Occlusion/range prevent collection through soil, and collected records remain absent across surface trips.
- Evidence: controlled excavation/exposure, occlusion, capacity and repeated-interaction checks. Author all starter entries as minor/common with detector eligibility false; larger size, metal content, higher price and nearby repeats cannot override the exclusion. `10` later checks audio/non-audio silence against actual non-minor targets from `42`; common rocks remain silent and cannot supply a positive detector case.
- Before implementation: read [progression/discovery findings](../../research/player-review-findings.md#progression-and-discovery), inspect the current 60% exposure/held pickup under the crosshair and test partial silhouettes at weak/strong shovel levels. Use [89's exact starter roster/counts/specifications](89-starter-minor-find-design.md) with [57's presentation brief](57-presentation-design.md), and use recorded approval for the specific replacement source/license/files/rollback batch. `109` already has trial integration approval; do not ask for it again. Any changed exposure or recognition delay still needs a separate evidence-backed decision; no tedious final cleaning or restored instructional HUD text.
- Next: `11` and the common-only production trip `15`; detector `10` waits for `42`'s non-minor content. Retain saving through `35`; count starters once in `40`/`41`. This task includes their normal release eligibility, replacing development-only art rather than delivering invisible release finds.
- Validate [89's common-tier rationale](89-starter-minor-find-design.md) through partial uncovering and rapid digging and held pickup directly under the crosshair at ordinary/stronger shovel levels. Record recognizable shapes, pleasant repetition and useful income; send weak silhouettes or awkward collection back to their owners. Common bottles/rocks need not be individually memorable; do not replace them with higher-tier toys to force surprise. Preserve 60% exposure and direct-aim held pickup (`112`); do not require release/repress or collect off-aim loot to preserve speed.
- Implement `57`'s selected recognition/feedback matrix within the existing collection flow. Exercise multiple quick pickups, last free slot/full bag, oblique partial exposure, muted audio and a competing low-reserve/rescue notice; verify identity is understandable and persistent inventory facts agree. Record actual notice/recognition failures before proposing a delay, threshold change or separate inspection action.
