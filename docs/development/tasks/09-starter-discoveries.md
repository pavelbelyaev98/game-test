# Task 09 - Production starter finds and collection presentation

Type: implementation. Status: `planned`. Prerequisites: 05, 06, 07, 08, 57, 89.

Feature: [discovery collection](../../features/backlog/discovery-collection.md). [Queue](../tasks.md). [Research basis](../../research/player-review-findings.md).

`89` is designing **three common item types**, all detector-silent. Consume its eventual selected specifications/counts from the revised alternatives, and [105's accepted art style](../../features/backlog/art-style.md) through `57`. No named replacement trio is selected yet; the later full roster remains separate.

## Task contract

- After `08` and production acceptance in `05`–`07`, retain the implemented exposure/collection system and seeded field; replace the three development forms with a coherent approved starter set in MainGame. Visuals must come from Blender MCP or free commercial-use sources. Do not recreate collection or disable working seeded placement; richer generation belongs to `45`.
- Acceptance: finds require their authored exposure threshold and actual visibility. Holding left-click collects the aimed eligible find exactly once without releasing between digging and pickup; full inventory leaves ordinary loot intact. Occlusion/range prevent collection through soil, and collected records remain absent across surface trips.
- Evidence: controlled excavation/exposure, occlusion, capacity and repeated-interaction checks. Author all starter entries as minor/common with detector eligibility false; larger size, metal content, higher price and nearby repeats cannot override the exclusion. `10` later checks audio/non-audio silence against actual non-minor targets from `42`; no fourth starter or common target is needed to force a positive detector case.
- Before implementation: read [progression/discovery findings](../../research/player-review-findings.md#progression-and-discovery), inspect the current 40% exposure/held pickup and test partial silhouettes at weak/strong shovel levels. Use [89's exact starter roster/counts/specifications](89-starter-minor-find-design.md) with [57's presentation brief](57-presentation-design.md), and obtain approval of the specific source/license/files/rollback batch. Any changed exposure or recognition delay still needs a separate evidence-backed decision; no tedious final cleaning or restored instructional HUD text.
- Next: `11` and the common-only production trip `15`; detector `10` waits for `42`'s non-minor content. Retain saving through `35`; count starters once in `40`/`41`. This task includes their normal release eligibility, replacing development-only art rather than delivering invisible release finds.
- Validate [89's common-tier rationale](89-starter-minor-find-design.md) through partial uncovering and rapid held pickup at ordinary/stronger shovel levels. Record recognizable shapes, pleasant repetition and useful income; send weak silhouettes or awkward collection back to their owners. Common bottles/rocks need not be individually memorable; do not replace them with higher-tier toys to force surprise. Preserve the approved 40% threshold and pickup timing.
- Implement `57`'s selected recognition/feedback matrix within the existing collection flow. Exercise multiple quick pickups, last free slot/full bag, oblique partial exposure, muted audio and a competing low-reserve/rescue notice; verify identity is understandable and persistent inventory facts agree. Record actual notice/recognition failures before proposing a delay, threshold change or separate inspection action.
