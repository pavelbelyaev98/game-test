# Task 73 — Concise task navigation

Type: maintenance; documentation only. Status: `done`. User-selected. [Completion](../completed/73-concise-task-navigation.md).

- Use `status.md` as the new-session entry point with direct active/next links.
- Keep `tasks.md` as unfinished work in priority order, one short linked row per task. Preserve task IDs, complete specs, research, decisions and delivery records.
- Remove repeated startup instructions from the handoff and reusable prompt; keep detailed policy in `AGENTS.md`.
- Acceptance: every previous unfinished task retains its order and spec; completed/cancelled records remain accessible; links and startup instructions agree.
