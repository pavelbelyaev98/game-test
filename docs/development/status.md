# Status

- Active: none. Next eligible: [56 — equipment progression design](tasks/56-progression-design.md) (`ready`). Then [120 — Shave review](tasks/120-powered-excavation-feel-design.md) (`planned`). Queue: [tasks.md](tasks.md).
- Direction: design more distinctive earned progression; excavation guns are wanted with implementation deferred; Shave is liked while the four-mode trial stays admin-only (`Ctrl+Shift+F10 → Experimental excavation`, session-only). Normal Scoop is restored. Audio direction: ambient nature plus digging/action feedback; specific sounds need approval. The user owns terrain art.
- Build: [SomethingDownThere.exe](../../builds/windows/SomethingDownThere.exe) — 2026-09-13, zero errors. Normal digging, admin Shave and the admin notice inspected via CLI; 156 relevant checks pass; all 45 existing profiles preserved; v6 saves drop only the experimental selection.
- Implemented anchors: 24 × 24 × 32 m mineral field with 1,024 finds (Coal → Diamond, `145`); independent shovel/backpack/battery tracks with paid refills (`138`/`48`/`46`/`139`); 60% aimed exposure, immediate held/walk-over collection and optional RMB handling (`136`/`131`/`115`); Sunny r8 ground + grass (`105`/`135`).
- Content decisions open: `117` unique-find purpose, `40` roster, `96`/`97` handling, `155` early pacing; `148` gates bulk content.
- Paused/blocked: `68` underground lighting paused; `80` blocked on user-deferred performance qualification; `137` awaits the user's verdict on one reservoir section (preview stays outside Assets — do not build or import more). `54` release qualification and user enjoyment remain open.
- Preserve the recovered user scene `unity/Assets/_Recovery/0.unity`, user-owned imports and current saves. Do not commit.
