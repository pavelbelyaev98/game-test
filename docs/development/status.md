# Current status

- Latest delivery: [116 - Stable held and dropped finds](completed/116-find-hold-and-rest-stability.md), `done`. Carry tracking keeps up with normal jetpack/sprint motion and clears the rotated hand target from soil/props. Low-speed contact solving and supported sleep stop persistent rocking; digging away support still wakes the find. `115`'s 60% exposure, 0.6-second recognition, RMB lift/drop and fresh LMB throw remain selected.

- Rock sources: [Blender source/exports/specs](../../art/photo-rock/README.md), three distinct silhouettes, three 2K maps per appearance, 12,500-triangle visuals and 82-triangle convex hulls. Original `113` is superseded by this integrated batch. [Three-shape preview](../../art/photo-rock/previews/variations.png).

- Next: [106 - Complete UI/UX state/text audit](tasks/106-ui-ux-audit-design.md), `ready`, then [107 - reviewed cleanup](tasks/107-ui-ux-cleanup.md), `planned`. Inventory every visible text, conditional screen/action and navigation/error state with visual evidence and editable decisions; the full audit/cleanup has not run.
- Collection/handling: [current contract](../features/backlog/discovery-collection.md). Keep 60% exposure, direct visibility/3 m reach, capacity and one inventory identity. Observation of a buried sliver never counts; looking away/occlusion or menu/focus loss resets it. Fresh deliberate collection can be immediate. Lifting works with a full bag, costs no fuel and never collects; throwing clears held/toggle intent. `115` refines `112`; no release/repress is required for automatic collection.
- **Trial art, not final model/style acceptance.** The user expects to supply better models. Start replacements from the [replacement guide](replacing-find-models.md), [bottle catalog](../../art/starter-finds/catalog.json), [rock catalog](../../art/photo-rock/catalog.json) and linked contracts. Sync preserves stable item/appearance IDs and asset references while regenerating geometry, materials, colliders and exposure samples. [Asset ownership/removal](../asset-ledger.md).
- Windows build: [SomethingDownThere.exe](../../builds/windows/SomethingDownThere.exe), **2026-09-10 16:11 UTC**, zero errors. One existing Pipeline runtime-tooling configuration warning. Includes stability `116`, handling/recognition `115`, rocks `114`, bottle physics `110`, sprint `87`, input accessibility `78` and current terrain saving `80`.
- Evidence: **180/180** relevant checks: 138 EditMode, 14 physics/handling, 17 discovery and 11 save. Twelve seeded drops settle; all three rock shapes retain the hold through pitch changes. MainGame rest stayed motionless for 23.9 seconds; jetpack/sprint/look review climbed 33.7 m at 8 m/s with the rock still held within 1.42 m. Captures inspected, user save preserved, clean MainGame/no console errors. [Results/captures/build report](../../unity/Logs/Task116/). Prior UI/input/startup evidence remains in `115`.
- Physics: bottles and rocks anchor while embedded, then fall/tip/settle; optional lifting uses the same physical object/collider. Pause freezes it; checkpoints save its released world pose and reload with empty hands. Return/rescue/reset releases before moving the player. Motion alone dirties autosave; v1/v2/v3 readers remain supported. [Contract](../features/backlog/discovery-collection.md).
- Trial review: [101](tasks/101-collection-return-playtest.md) records the user's `115` drop-jitter/flight failures as NOT OK, with `116` ready for user retest. Recognition, shapes and other feel verdicts remain open. Final art remains `09`, coordinated style `105`; `96` retains other category physics.
- Art design: [105 - Art-style research and texture trials](tasks/105-art-style-texture-trials.md), `ready`. Compare multiple coordinated packs in-game until the user selects one, using the [preferred reference](../features/backlog/art-style.md). Ground texture trials and final style remain open; `57`/`08` consume this owner. Site selection remains `58`; common-only `15` precedes later `42` -> `10` -> `102` detector validation.
- Player decisions/playtests: [95 - ideas and feel plan](completed/95-player-ideas-and-playtest-plan.md) maps [nine ideas](../research/player-idea-assessment.md) to design `96`-`99` and existing `40`/`71`; bottle/rock physics and optional lift/drop/throw are implemented in `110`/`114`/`115`, while other optional mechanics remain proposed. [User playtests](playtesting.md): `100`/`101` ready on the current build, `102`-`104` await production systems; user verdicts remain untested. Horizontal-travel criticism concerned Meltopia, not this build.
- Research context when needed: [634-review audit](../research/steam-review-audit/report.md), [player-review findings](../research/player-review-findings.md), and [risks/intended feelings/what to avoid](design-risks.md). These inform decisions; recommendations do not automatically select mechanics. Warning `93`/`94` and portability `91` remain future work.
- Paused: [56 - Equipment progression](tasks/56-progression-design.md) and [68 - Underground lighting](tasks/68-underground-lighting-design.md) remain `in_progress` at the user's explicit request. Follow the direct next link to `106` rather than auto-resuming either. Lighting's proposal remains unselected.
- Deferred: [80 - Incremental terrain saving](tasks/80-incremental-terrain-saving.md), `blocked` on user-deferred performance qualification. Its code is integrated; do not restart the long benchmark without a new request or mark it done from functional checks. Prior native save/quit/relaunch evidence remains in `unity/Logs/Task80/`.
- Existing controls: held Left Shift gives modest sprint, with crouch priority. **Controls -> Digging mode -> Toggle** starts/stops digging on fresh presses; menus/focus loss clear it. Hold is default. Both modes collect directly aimed eligible finds after recognition; fresh deliberate collection remains available. **Lift / drop find** is rebindable; throwing follows Dig and requires a fresh press. Hints and Pause show current bindings.
- Workflow: [validation policy](../scope-and-validation.md#validation-policy), including simulated-input regression and brief announced native reviews. Preserved crash-recovered user scene: `unity/Assets/_Recovery/0.unity`; keep it out of asset rollback. Sandbox `76` is cancelled; Windows repair is not a prerequisite. Do not commit.
- Release: [63's Windows targets](../features/backlog/release-validation.md#windows-support-targets) guide implementation; final hardware/full-run qualification remains [54](tasks/54-release-qualification.md), edit spikes `25`, Steam `53`. Final world art/HUD/comfort acceptance remains `08`/`05`/`57`/`54`.
