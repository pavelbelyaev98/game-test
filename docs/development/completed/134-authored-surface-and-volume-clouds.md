# 134 - Deliberate surface layout, stones and 3D clouds

Current appearance is superseded by [135](135-restore-grassy-site.md): the user restores continuous grass and removes clouds and decorative surface stones. Retain this record as prior evidence only.

- Why: the user rejected the random grass islands and flat-looking clouds, requested more grass/surface stones, and asked whether FPS drops were eliminated.
- Integrated: about **84.5%** of the site is grassy, around a winding exposed strip and narrower western branch. Modest edge irregularity replaces noise-defined islands. The existing rooted wind, blade-preserving distance detail and compact batches remain: 6,678 clumps, roughly 0.83M visible triangles / 7 grass submissions in the reference view.
- Stones: 49 partly embedded 16-34 cm stones in seven deliberate groups reuse approved B/C artwork through 250-triangle Blender derivatives and the original textures. Two instanced groups add no loot identities/colliders; local digging clears unsupported stones and reset/restore regenerates them.
- Clouds: four full-depth Blender volumes at 150 m altitude replace the sky-atlas projection. Pale baked vertex tint, fixed world orientations and slow shared translation give distinct side/underside views without camera-facing behavior. Bright sky, sun and world lighting remain; source/rollback are in the asset ledger.
- Evidence: fast compile and four relevant scene/layout/grass/dressing integration tests pass. Official Unity CLI inspection covers the surface, stones, two sky directions and cloud side/underside views; a visible scoop clears its stone group (49 to 42). Temporary captures are cleaned; source/game artwork remains.
- Performance: paired 1440p DX12 native captures on RX 9060 XT / Ryzen 9700X measure about **0.62 ms** added grass GPU cost in the first rendered run, versus 1.91 ms before the grass optimization. Movement medians vary **4.0-6.0 ms** across runs. One first-run 41.1 ms wall-time outlier had no corresponding grass submission/GPU spike; it did not repeat in two instrumented movement passes (worst 8.3 ms). Latest sampled digging peaks at 11.6 ms/frame; terrain removal/mesh work costs up to ~8.1 ms even with grass off. [Native reports and limits](../../../unity/Logs/Task134/performance-summary.json).
- Native method: separate additive MainGame without owner save writes, fixed view plus 8-second movement/turn sweeps and 16 real terrain cuts per grass state. Latest diagnostic records 150% render scale / 1x MSAA; shared-PC run-to-run variability remains. The hidden-window attempt with no draws is discarded; this is not a claim of zero hitches, save-IO performance or minimum-hardware qualification.
- Windows build: **2026-09-12 19:20 UTC**, zero errors / one existing Pipeline runtime warning; clean seven-second native startup. No commit. Next ready work is 126; reservoir 130 retains its pending specific art decision.
