# 64 — Camera comfort design

- Selected the current 75° vertical view, a 55–90° slider and a steady white crosshair by default; the user accepted proceeding to implementation.
- The [owning contract](../../features/backlog/camera-comfort.md) retains the reasons, input isolation, independent preferences, reset/error behavior and future motion-effects rules.
- Official CLI review compared passage/shaft views at three FOVs and two aspect ratios, confirmed unchanged center rays and sampled the actual existing dig pulse (`unity/Logs/Task64/`). MainGame remained clean.
- [65](../tasks/65-camera-comfort-settings.md) implements and validates the native controls. Future tool clipping and final long-session comfort remain with equipment work and `54`.
