# Task 120 - Earlier expressive excavation comparison

**Superseded by user review:** Shave liked; other trial modes unconvincing; previous normal digging preferred for now. The current package and tool are experimental only. **Excavation guns are explicitly wanted for later implementation.** Task `120` is reopened for progression details; `154` removes sounds and isolates the trial.

- Why: the user requested a playable way to judge the feedback and delegated the needed design/creation.
- Selected: one homemade excavator, existing Scoop plus Bore, Fan and Shave, available immediately and scaled by existing paid tiers. Remappable Q, release-safe switching, per-mode fuel/cadence and saved selection are recorded in the [owning contract](../../features/backlog/excavation-modes.md).
- Omitted: firearm inventory, second tool, breaker and suction; automatic cleanup already removes the latter's proposed chore. Full independent tracks and installation staging stay with their owners.
- Evidence: `147` integrates the comparison in MainGame and the normal [Windows build](../../../builds/windows/SomethingDownThere.exe); [validation](../../../unity/Logs/Task147/validation-summary.json) covers geometry, input, collision, collection and saving.
- Remaining: `56`/`120` define progression and Shave's role in the intended future guns. `147` retains deferred implementation and its measured edit-budget gap; `100` records the trial verdict and `148` owns the later combined discovery slice.
