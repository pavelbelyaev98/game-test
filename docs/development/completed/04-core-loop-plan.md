# Task 04 - Core loop and main game scene plan

Why: turn the working FPS foundation into an ordered path toward one complete excavation trip without designing every production system.

Integrated result: the [owning feature](../../features/backlog/core-loop.md) defines main-scene owners, initial content, session lifetime and normal-return/overextension proof branches. Tasks `06`-`14` form the implementation sequence.

Evidence: documentation links, task order, ownership, acceptance, runtime references and document lengths passed checks. This was explicitly documentation-only, so no runtime tests were needed.

Remaining limitation: the complete loop is not integrated. Continue from Task `07`; disk persistence, generated placements and final presentation remain deferred.
