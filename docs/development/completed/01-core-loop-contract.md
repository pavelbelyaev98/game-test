# 01 - Core loop and first-playable scene plan

Why: turn the working FPS foundation into an ordered path toward one complete excavation trip without designing every production system.

Integrated result: the [owning feature](../../features/backlog/01-core-loop-contract.md) defines scene objects/owners, temporary fixtures, session lifetime and normal-return/overextension proof branches. Nine implementation slices have scoped acceptance in their owning features; `03a` is ready. Full-feature tasks remain planned.

Evidence: local documentation links/anchors, unique queue IDs, slice order/ownership/acceptance, existing runtime references and document lengths passed checks; `git diff --check` passed. Task `01` was explicitly documentation-only, so no runtime tests were needed or rerun.

Remaining limitation: the playable loop is planned, not implemented. Begin `03a`; resolve Unity MCP access before live scene/editor validation. The initial proof intentionally defers disk persistence, generated placements and final presentation.
