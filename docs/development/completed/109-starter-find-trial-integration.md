# Task 109 - Replaceable starter-find trial integration

Current content revision: [110](../tasks/110-bottle-physics-and-recognition.md) retains only three enlarged bottles, adds 60% reveal/physics and removes can/brick assets. Original delivery counts below are historical; use the current batch/catalog for implementation.

- Why: the user explicitly approved integrating the revised seven Blender models now, with better replacements expected later. This completes trial integration; final production art/style acceptance remains `09`/`105`.
- Integrated: MainGame uses seven textured bottle/can/brick prefabs, exact 72 new-game allocations (24 shallow), 2/1/3-credit prices, real metre dimensions, centred mesh colliders and 256 exterior exposure samples per variant. All remain common, one slot, silent to the detector and collected through existing held interaction.
- Replacement: [editable source catalog](../../../art/starter-finds/catalog.json) -> repeatable sync -> stable runtime catalog/prefabs. [Model/reference/spec replacement guide](../replacing-find-models.md) covers sources, every reference, dimensions/maps, gameplay changes, saved identity, licenses and precise removal. [Ledger](../../asset-ledger.md) records recursive ownership and trial approval.
- Compatibility: legacy 96-find saves retain instance IDs/centres, collected absence, carried records and historical values. Old content GUIDs map to stable category IDs with unit model scale; copied migration is idempotent and unknown content blocks load. Save readers v1/v2 are unchanged.
- Evidence: **133/133 EditMode**, **13/13 discovery**, **7/7 save**, **7/7 startup** tests; save checks include legacy checkpoint recovery and reload. Repeat sync preserves **40 GUIDs**. Seven FBX reimports retain **9,508 triangles**, correct dimensions/UVs; all seven colliders enable build prebaking.
- Playable evidence: actual MainGame contains 72 finds/seven variants; **14/14** weak/strong digging checks collect and sell each variant exactly once. In-soil bottle/can/brick views inspected with the official CLI. [Evidence directory](../../../unity/Logs/Task109/), including test results, pickup report and close screenshots.
- [Windows build](../../../builds/windows/SomethingDownThere.exe): **2026-09-10 08:39 UTC**, succeeded, zero errors. One existing Pipeline warning concerns absent runtime tooling configuration; the collision warning is resolved.
- Limitation/next: real-sized finds can be obscured by the excavation rim and worn writing requires close inspection; user feel/readability verdicts and final art approval remain open. No rigidbody behavior was added (`96` owns that design). Resume UI audit `106` next; use the replacement guide when better models arrive.
