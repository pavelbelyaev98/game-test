# Asset and audio ledger

## Task 145 - depth-progression minerals

- Approval: user explicitly answered **"Approve this eight-mineral batch"** to the preview of Coal, Copper, Iron, Silver, Gold, Emerald, Ruby and Diamond, including textures and collision models. Scope is these eight buried sellable finds and their catalog integration; no scenery/audio.
- Source/license: original Blender MCP geometry, procedural materials and baked textures; `art/minerals/Minerals.blend` and `create_minerals.py`. `art/minerals/LICENSE.txt` permits commercial use, modification, redistribution and sale without attribution; no third-party art/audio/font files. Preview labels/lights/backdrop are source-only and excluded from Unity imports.
- Source ownership (recursive): `art/minerals/`, including `.blend`, recipe, editable `catalog.json`, manifest, per-mineral FBX visual/collision exports, BaseColor/Normal/Masks PNGs, README and license. `.blend` is automatically LFS tracked.
- Unity ownership (recursive): `unity/Assets/Content/Minerals/` plus `Minerals.meta`; Models, Textures, Materials, Meshes, Prefabs, `RockContact.physicMaterial`, license and every folder/importer/asset `.meta`. Stable `mineral_<name>` save keys; one slot and 60% exposure. No Resources loader, fonts, audio settings or ThirdPartyNotices entries.
- Owned importer: `unity/Assets/Editor/MineralSetup.cs` and `.meta`. Shared integration: `StarterFindSetup.cs` (source depth fields and append call), existing `Content/StarterFinds/StarterFindCatalog.asset`, `Scenes/MainGame.unity` (population count), and `art/photo-rock/catalog.json` (96 retained shallow rocks). DiscoveryCatalog/DiscoveryField and catalog/placement/collection/save tests own reusable depth placement and validation. `Runtime/Terrain/ExcavationDaylight.cs`/`.hlsl` and its integration test tune existing ambient minimum to 0.45 for deeper readability; restore 0.14 in both implementations and matching expectations to undo that tuning independently. Existing rock/bottle art and user terrain are separately owned and preserved.
- Removal: first set mineral source counts to zero and sync to stop new spawning while retaining existing-save resolution. Prefer a visual replacement preserving each `mineral_<name>` key. Full asset removal requires a tested content alias for all eight IDs to retained approved visuals; then remove the mineral catalog entries, importer/append call, owned folders and metas, restore rock counts to 336/264 in its source, sync catalog/scene and rebuild Windows. Keep historical saved names/values/identities, all user saves, and the independent depth/migration changes. Never reset excavation to remove artwork. Remove this batch's license with its owned folders; there are no shared notice/loader entries to leave behind.

## Task 137 - staged first reservoir section

- State: source preparation explicitly requested; specific section approval and integration are pending. The user rejects 130's complete environment batch and wants one part reviewed before any more reservoir construction. Old 130 previews are retired; sources/exports/license remain inactive.
- Owned staging recursively: `unity/Logs/Task137/section/` (Blender source/recipe, one dam/outlet FBX, baked base/normal/surface maps, preview, manifest, README and original-art commercial/no-attribution license). Temporary reference photographs live under `unity/Logs/Task137/references/` for inspection only and will be deleted; their URLs and observed details remain in 137's review. Reference pixels never enter model textures or exports.
- Intended isolated integration after approval: `art/reservoir/first-section/` recursively; `unity/Assets/Content/Reservoir/FirstSection/` recursively and new parent/folder metas, model/importer subassets, baked maps, URP material, license and all metas; a section setup script/meta under `Editor/`. Shared integration would edit MainGame's new reservoir-section instance, builder setup hook and relevant scene checks. It would preserve boundary authority, current grass/sun/clear sky, station positions and saves. No files from this section enter Assets before approval.
- Removal after integration: remove only the section instance and its builder/test/setup references through Unity; restore any boundary renderer visibility explicitly changed by that integration; remove the isolated imported section files/metas and empty parent folder/meta, plus its setup/meta. Source removal is optional; do not restore the rejected clouds/trees/river/ground rocks or touch other reservoir sections, original terrain, finds or saves. Current staging alone has no gameplay effect.

Completed review galleries and preview renders were removed at the user's request in 133. Actual game/source textures, editable Blender files, exports, licenses and written approval records remain. Only the current first-section approval preview in Task137 remains pending; the rejected Task130 gallery is removed.

## Task 135 - requested scenery removal and continuous grass

- Authorization: the user explicitly requests removing clouds, trees, river/water and decorative ground rocks, and restoring the whole grassy land patch. 133/134's clearing compositions are retired; optimized grass meshes, batching, wind and approved ground maps remain in use.
- Removed through Unity: MainGame's `Clouds` and `Scenery` groups (four clouds, four tree pairs, water and six exterior decorative rocks), `SurfaceStoneRenderer` on the terrain, and camera references to cloud content. Camera far clip returns to 180 m and local sky uses `Content/Sun/SunnySun.mat`; its existing shader/setup now retain the accepted bright cyan gradient. Shared `MainGameSceneBuilder.cs` and `MainGameSceneTests.cs` no longer recreate/require retired scenery. Original `Settings/MainGame/{Water,Bark,Foliage}.mat` and metas are retained solely because the user-owned `_Recovery/0.unity` still references them; that scene is untouched.
- Removed runtime ownership, including all metas: `unity/Assets/Content/Clouds/` recursively and folder meta; `Content/PhotoRocks/Models/SurfaceStones.fbx`; `Editor/{CloudPresentationSetup,SurfaceStoneSetup}.cs`; `Runtime/Terrain/{CloudDrift,SurfaceStoneRenderer,SurfaceVegetation}.cs`; `Runtime/Terrain/SurfaceVegetation.hlsl`; `Tests/PlayMode/SurfaceDressingIntegrationTests.cs`; `Tests/EditMode/SurfaceLayoutTests.cs`. These relative paths are under `unity/Assets/`. The shared ground shader no longer includes a clearing mask; existing grass integration tests cover continuous support. B/C rock material instancing flags return to false; original collectible rock assets and identities are unchanged.
- Retained inactive source ownership: `art/clouds/` recursively (original Blender forms/atlas, volume FBX, recipes, source checkpoints, README and commercial/no-attribution license); `art/photo-rock/{export_surface_stones.py,SurfaceStones.blend,SurfaceStones.fbx}` (134's 250-triangle B/C derivatives, under the existing rock license). No runtime loader, scene reference, duplicate license notice or importer remains for the retired features. Removing these inactive source files later has no game effect; preserve original collectible rock variants and independent `art/sun/` / `art/ground-grass/`.
- Earlier authorization remains recorded below for source provenance, not current integration permission. Do not automatically restore the rejected clouds/clearings/stones from checkpoints. Existing sun/grass removal steps below still apply; preserve the current cyan background when removing only the sun. A new requested scenery revision must explicitly update current integration/removal ownership.

## Task 130 - rejected reservoir source archive

- State: the user rejects the complete environment batch as low quality and requests section-by-section review in 137. Its original approval request is retired; no reservoir asset has been imported.
- Retained inactive ownership recursively: `unity/Logs/Task130/reservoir-preview/` contains original `Reservoir.blend`, `Reservoir.fbx`, recipe, six baked color maps, manifest/material bindings, license, README and the obsolete prepared setup. Original Blender MCP artwork (three meshes / 28,120 triangles); `LICENSE.txt` permits free commercial use/modification/redistribution/sale without attribution. No reference pixels were used.
- Retired review ownership: `docs/development/cloud-reservoir-review-130/` HTML/two images and the old staging `player-preview.png` / `overview.png` are removed now that the proposal is rejected. Their pending links are removed; actual source textures stay.
- This archive has no scene/code/config integration to undo. Removing it later has no game effect. Do not run its old setup or restore removed scenery; approved future section ownership is recorded separately under 137.

## Task 130 - retired approved cloud source

- Original approval: the user said **"add clouds also, I approve it"** on 2026-09-12; 132/134 refined the same Blender forms at their explicit request. 135 later requests full game removal, now completed.
- Source/license: `art/clouds/Clouds.blend`, created through Blender MCP. Retained recursive source ownership and runtime removal are listed above. `art/clouds/LICENSE.txt` permits free commercial use, copying, modification, redistribution, sublicensing and sale, without attribution; no reference-game pixels were imported.
- `previous-130.zip` / `previous-132.zip` retain historical source/runtime checkpoints. They are inactive archives, not an integration dependency or a selected rollback. Cloud runtime folders, setup, drift and MainGame references are absent; the independent sun owns the accepted clear gradient.

## Task 128 - approved visible sun

- Approval: the user replied **"Approve this sun"** on 2026-09-12 to the specific Blender preview: one cream/yellow disc with a restrained halo, aligned with the existing sunlight, preserving the blue sky and lighting.
- Source/license: original Blender MCP artwork, `art/sun/Sun.blend`; `art/sun/LICENSE.txt` grants free commercial use, copying, modification, redistribution, sublicensing and sale, with no attribution. No third-party content or audio.
- Owned source recursively: `art/sun/` (Sun.blend, create_sun.py, Sun_Disc.png, README.md, LICENSE.txt). Owned runtime recursively: `unity/Assets/Content/Sun/` plus `Sun.meta` (Sun_Disc.png, SunnySun.shader, SunnySun.mat, license and every importer/file meta).
- Owned setup: `unity/Assets/Editor/SunPresentationSetup.cs` and its meta. Shared integration: `unity/Assets/Scenes/MainGame.unity` camera clear mode/local Skybox component; `unity/Assets/Editor/MainGameSceneBuilder.cs` setup call; `unity/Assets/Tests/EditMode/MainGameSceneTests.cs` sun checks. 135 moves the accepted clear cyan gradient into the same SunnySun shader/setup/material and restores its camera reference. No runtime loader or extra geometry/collider/light is added.
- Removal: remove the camera's local Skybox component through the Editor, restore its SolidColor clear mode while retaining its background color, and save MainGame. Remove the SunPresentationSetup builder call and sun-specific assertions; delete the owned setup/meta, Sun content folder/meta and, if requested, the source folder. Rebuild Windows. Preserve the existing Sun directional light, ambient/reflection settings, ground, grass, player saves and the independent 128 grass-density correction.

## Task 127 - approved moving surface grass

- Task 133 refinement: the user explicitly requests bare-earth openings, taller growth areas and a cheaper approach. Existing Blender FBX/source is refined in place: the far mesh retains all twelve original blade roots/tips with 72 triangles instead of 168; near mesh and atlas stay intact. `art/ground-grass/optimize_grass.py` and `previous-129.zip` belong to this source folder. The checkpoint retains pre-133 source/exports and renderer/setup/ground-shader files, without extra preview images.
- 133 shared integration: `SurfaceGrassRenderer.cs` batches the stable growth patches and selects geometric detail; `SurfaceGrassSetup.cs` authors density/far-mesh references; MainGame serializes those references. The shared clearing field/include/metas added by 133 were removed in 135; `Content/GroundTextures/GroundTriplanar.shader` again covers the original surface with turf. Existing approved soil/turf textures supply the openings; their ownership is unchanged.
- Revert only 133 grass: restore the checkpoint's existing owned files, remove SurfaceVegetation code/include/metas, refresh Unity, remove the obsolete farMesh serialized reference via the Editor, run the restored grass setup and explicitly set cellsPerPatch to 12 on MainGame grass, then save/rebuild. Restore pre-133 grass integration assertions/docs. Preserve all source/runtime `.meta` identities, pickup changes and unrelated gameplay. 135 already removes those field files/include and restores uniform turf; skip that obsolete removal step.
- Approval: on 2026-09-12 the user replied **"Approve this grass batch"** to the staged Sunny preview and the explicit 12-blade clump, six-blade distance mesh, baked green atlas, gentle wind material and MainGame integration/removal proposal.
- Source/license: original Blender MCP authoring; `art/ground-grass/GroundGrass.blend`. `art/ground-grass/LICENSE.txt` grants free commercial use, copying, modification, redistribution, sublicensing and sale, with no attribution. No third-party artwork or audio.
- Refinement 129: the user explicitly requests taller, prettier grass and natural wind on 2026-09-12. Refined the existing approved twelve-blade mesh/atlas/material in place through Blender MCP, with `art/ground-grass/refine_grass.py` retained and embedded in the source; file names, source/license and import metas remain. `art/ground-grass/previous-approved.zip` retains the pre-refinement source/runtime files at repository-relative paths; restore only its listed source/runtime/test files, remove `refine_grass.py`, reimport through the Editor and run Configure Approved Surface Grass to reverse 129's mesh/texture/wind/bounds changes. Preserve later unrelated edits, the sun and shovels; rebuild Windows and update the current grass guide when reverting.
- Current renderer: 133 retains twelve blades at both detail levels, uses full supported-surface coverage with seeded spacing/height variation and compacted instanced batches after 135. The former six-blade distance mesh is replaced within the existing approved FBX.
- Owned source, recursively: `art/ground-grass/` (editable blend, `create_grass.py`, `GrassClumps.fbx`, `Grass_Albedo.png`, README/license). The blend's review ground references the separately owned accepted turf; that texture and original Blender scene are preserved.
- Owned runtime, recursively: `unity/Assets/Content/GroundGrass/` and `GroundGrass.meta`, including FBX/imported mesh subassets, atlas, `SunnyGrass.mat`, `GrassWind.shader`, license and all file/folder/importer metas.
- Owned code/tests and their metas: `unity/Assets/Runtime/Terrain/SurfaceGrassRenderer.cs`, `unity/Assets/Editor/SurfaceGrassSetup.cs`, `unity/Assets/Tests/PlayMode/SurfaceGrassIntegrationTests.cs`. Shared integration: `unity/Assets/Scenes/MainGame.unity`, `unity/Assets/Editor/MainGameSceneBuilder.cs`, `unity/Assets/Tests/EditMode/MainGameSceneTests.cs`. No Resources loader, physics collider, font, audio or ThirdPartyNotices entry.
- Removal: remove SurfaceGrassRenderer from MainGame through Unity and save; remove the builder setup call and grass-specific scene assertions; delete only the listed owned scripts/metas and GroundGrass folder/meta, then rebuild Windows. Delete the isolated source folder only if removing sources is also requested. Preserve GroundTextures, existing user scenery/stations/finds, all saves, and the independent shovel-tuning hunks in MainGame/code/tests/docs.

Approved content and its removal boundaries are recorded below. The user owns the terrain work, including the explicitly requested original texture batch below. The removed presentation pass and TextMeshPro resources remain inactive.

## Task 126 - temporary rock-only population

- User requests a rock-only field with more top-layer finds (`126`, then `140`). Bottle source counts are 0/0; all source/import files, licenses, metas, prefab keys and legacy aliases remain retained for existing saves. Current rock source counts are 336 total / 264 shallow, at 2 credits per new rock. The three approved appearances retain their geometry/materials; the existing prefabs carry the selected sale value.
- Integration edits: `art/{starter-finds,photo-rock}/catalog.json`, `unity/Assets/Content/StarterFinds/StarterFindCatalog.asset`, `Content/PhotoRocks/Prefabs/common_rock_{a,b,c}.prefab`, `Scenes/MainGame.unity`, `Editor/StarterFindSetup.cs`, `Runtime/Interaction/DiscoveryCatalog.cs`, and the catalog/discovery/physics/save/startup compatibility checks under `Tests/`. Shared test fixture: `Tests/PlayMode/TestInputPreferences.cs`. Unity paths after the first are relative to `unity/Assets/`; existing metas remain untouched.
- Reversal of this population change: restore the prior bottle quotas (219/200, 176/160, 109/96 total/shallow) and rock quota (48/24) plus its 1-credit price in the two source catalogs, run **Sync Starter Find Models**, then restore quota expectations and current contract text. Zero-count support may remain. No source/import files or save records need removal; do not reroll existing excavations. Re-enabling bottles later requires the user's requested direction.

## Tasks 113/114 - three appearances of one common rock

- Approval: the user explicitly requested this photo-referenced rock, then corrections, three appearances, random spawn rotations and game integration. **Only A retains one central hollow; B/C have solid uneven tops.** Narrow irregular undersides replace the rejected broad chest-like base. Trial integration is approved; final style/player-feel acceptance remains open.
- Source/license: original Blender MCP geometry/materials, using user photographs as visual reference without importing their pixels. `art/photo-rock/LICENSE.txt` permits commercial use, modification and redistribution without attribution. [Source, specs and replacement](../art/photo-rock/README.md); [artifact checks](../art/photo-rock/validation.json).
- Source ownership (recursive): `art/photo-rock/`, including `.blend` sources/backups, recipe/catalog/manifest, `variants/{a,b,c}/` visual/collision FBX, textures and validation/license. Blender data uses `SDT_PhotoRock` prefixes. Old single-model root exports/maps are superseded by the three canonical variant folders. Preserve existing Scene/SDT_Starter data and unrelated user work.
- Unity ownership (recursive): `unity/Assets/Content/PhotoRocks/` plus `PhotoRocks.meta`, including six FBX imports, nine maps, three URP materials, six centred meshes, three appearance prefabs, `RockContact.physicMaterial`, copied license and all importer/folder metas. Owned setup: `unity/Assets/Editor/PhotoRockSetup.cs` and `.meta`.
- Shared integration: `unity/Assets/Editor/StarterFindSetup.cs`, `Runtime/Interaction/{DiscoveryCatalog.cs,DiscoveryField.cs,BuriedFind.cs}`, `Content/StarterFinds/StarterFindCatalog.asset`, `Scenes/MainGame.unity`; `Tests/EditMode/StarterFindCatalogTests.cs`, `Tests/PlayMode/{FindPhysicsIntegrationTests.cs,DiscoveryIntegrationTests.cs,SaveIntegrationTests.cs,StartupMenuTests.cs}`. Shared docs: ledger, source/spec replacement guide, architecture, idea/starter/content/collection contracts, affected task/playtest files and status/queue.
- Handling updates `115`/`116`: source `catalog.json` owns `throw_speed`, synced to `FindPhysics`; carry/rest tuning belongs to runtime physics. Shared `FpsPlayer`/`FpsInput`/`InputPreferences`, `FindHandling.cs` (and meta), existing HUD/Controls files and input/physics/save tests own handling integration. `Tests/PlayMode/FindHandlingStabilityTests.cs` plus its `.meta` extends the shared physics test fixture. Preserve these generic systems when removing only the rock art; remove both partial physics test files together only when retiring that entire system.
- Identity/replacement: one catalog item `common_rock`; saved appearances `common_rock_a`, `common_rock_b`, `common_rock_c`. Keep these keys and existing metas while replacing geometry/maps. `art/photo-rock/catalog.json` owns price/slots/counts/dimensions/exposure/mass and export references; sync converts textures and regenerates centred meshes/hulls/exposure samples. Preserve source/license and update specs/validation together.
- Integration removal: first retain or explicitly migrate saved rock keys. Remove the `PhotoRockSetup.AppendToCatalog` hook from `StarterFindSetup`, remove the Rock catalog entry through the Editor, re-sync the bottle catalog and save MainGame. Then remove only the owned PhotoRocks folder/meta and setup script/meta. Keep generic appearance support, bottle entries/assets, terrain and saves; never delete a save to conceal missing content.
- Source removal: remove only `art/photo-rock/` (and its meta if created), owned `SDT_PhotoRock` Blender data and current source links. Preserve numbered/completed evidence, original user photos, terrain, bottle sources/imports and other open Blender work. No separate project notices or runtime loader was added.

## Tasks 108/109/110 - replaceable starter bottles

- Retained approval (new spawning disabled by `126` above): the user requested **only the three bottles, slightly larger, 60% exposure and physical falling when detached**, and full removal of cans/bricks. Task `110` revises the previously approved seven-model trial: bottles are 25% larger; retained models/materials are still **trial art, not final style acceptance**. Better replacements remain expected later.
- Source/license: original Blender MCP geometry/materials and original label layouts using Blender built-in outlines. No downloaded artwork or font file. `art/starter-finds/LICENSE.txt` permits commercial use, modification and redistribution without attribution; JulioVII's reference textures are not included. [Source/export guide](../art/starter-finds/README.md).
- Source ownership (recursive): `art/starter-finds/`, including `.blend` sources/backups, bottle/collision scripts, `collisions/` (3 convex FBX), collision manifest/validation, editable catalog, generated manifest, FBX, label-art masks, texture atlases, validation and license. Owned Blender scenes/datablocks use `SDT_Starter`; preserve original Scene/Cube/Camera/Light and other user work.
- Unity ownership (recursive): `unity/Assets/Content/StarterFinds/` plus `StarterFinds.meta`. Includes `Models/` (3 visual bottle FBX plus 3 `_collision.fbx` hulls), `Textures/` (3 bottle PNGs), `Materials/Bottles.mat`, `Meshes/` (3 centred visual meshes plus 3 `_collision.asset` hulls), `Prefabs/` (3 stable bottle prefabs), `StarterFindCatalog.asset`, `BottleContact.physicMaterial`, license, and every folder/importer `.meta`. Source-only labels/fonts/preview staging are excluded. The physical contact material controls friction/bounce and adds no visible art.
- Owned setup: `unity/Assets/Editor/StarterFindSetup.cs` plus `.meta`; sync reads `art/starter-finds/catalog.json` and maintains the above generated assets without replacing their GUIDs. There are no audio, Resources loaders, additional fonts or ThirdPartyNotices entries in this batch.
- Shared integration (relative to `unity/Assets/`): `Scenes/MainGame.unity`; `Editor/MainGameSceneBuilder.cs`; `Runtime/Interaction/{DiscoveryField.cs,BuriedFind.cs,DiscoveryCatalog.cs}`; `Runtime/Terrain/TerrainVolume.cs`; `Runtime/Persistence/{WorldSnapshot.cs,WorldSaveCodec.cs,WorldSaveController.cs}`; `Tests/EditMode/{StarterFindCatalogTests.cs,WorldSaveTests.cs}`; `Tests/PlayMode/{DiscoveryIntegrationTests.cs,SaveIntegrationTests.cs,StartupMenuTests.cs}`. Reusable new `Runtime/Interaction/FindPhysics.cs` and `Tests/PlayMode/FindPhysicsIntegrationTests.cs`, both with `.meta`. Preserve new-file metas for catalog/tests from `109`. Save version 3 adds released state and retains v1/v2 readers; content aliases preserve retired identities.
- Removed by `110`: all four can/brick model sources/exports, their owned Blender geometry/material/label/image datablocks, six can/brick texture maps, two previews and three label masks; corresponding four Unity models/meshes/prefabs, two materials, six maps and all their metas. Recipes retain bottles only; completed previews were cleaned under 133. Retired content strings remain exclusively for save compatibility, tests and concise removal/history records; they are not asset references or permission to restore removed art.
- Preserved separately: old `unity/Assets/Content/Finds/` prefabs/materials/metas, all user terrain/stations and user saves. Pre-session crash backups were kept as `unity/Assets/_Recovery/0.unity` with its `.meta` and `_Recovery.meta`; these are recovered user work, not starter assets and must not be removed as part of this batch.
- Replacement: follow the [complete reference/spec replacement guide](development/replacing-find-models.md). Edit source paths/maps/dimensions in the catalog, keep IDs, run sync, review partial burial and reload, and update owning docs. Models remain replaceable even after this integration task completes.
- Removal: prefer a compatible visual swap. For full removal restore the original three legacy prefab references, clear the catalog, restore 96/development gating and builder wiring through the Editor; remove the scoped physics component/test and revert field/find/motion-observation/terrain-restore hunks together, then remove the owned StarterFinds folder/meta and setup script/meta and rebuild. **Keep v3 save reading and content mappings** unless a tested downgrade is supplied; otherwise newer saves become unreadable. Remove source/owned Blender datablocks only if requested. Preserve all user work and saves; never reset a save to undo art.

## Task 77 - original soil and short turf

- Approval: user explicitly requested ground textures similar to A Game About Digging a Hole and said **"if creating u are allowed t ocreate it directly"**. This covers original soil/turf textures and their ground integration; online imports require a separate approval.
- Approved refinement: the user supplied a close-up and requested rocky texture instead of the uniform dots. Replace the existing soil maps with Blender-authored irregular embedded stones/mineral grit inside the same owned source/content folders; retain turf, geometry and existing asset GUIDs. Private provenance stays in development records; no authoring-tool credit is added to game UI.
- Subsequent approved tuning: fewer and less conspicuous stones with adhering dirt; rebake the same three soil maps from the retained recipe/source. Ownership and removal boundaries remain the same.
- `105` acceptance/cleanup (2026-09-12): user approved **Sunny r8** as “perfect”, then explicitly requested removing surplus images. Selected runtime/source set: r8 soil albedo, r7 soil normal/mask and r6 turf. Forty superseded PNG exports, 100 superseded gallery screenshots, 192 temporary image copies and 40 old packed cartoon bakes are removed. All editable geometry/materials/recipes, the six selected packed cartoon bakes, validation reports and non-image code/config baselines remain; final review captures were subsequently cleaned under the user's 133 policy. This approves neither moving grass nor final find/prop art.
- Source/license: original Blender MCP authoring; no third-party artwork. `art/ground-textures/LICENSE.txt` grants free commercial use, modification and redistribution without attribution. The [official screenshots](https://store.steampowered.com/app/3244220/A_Game_About_Digging_A_Hole/) are visual reference only, never game content.
- R4 natural-light integration (code, no new art): `Runtime/Terrain/ExcavationDaylight.cs`, `ExcavationDaylightGrid.cs`, `ExcavationDaylight.hlsl`, `ExcavationLit.shader` and their `.meta` files; `Tests/EditMode/ExcavationDaylightTests.cs`, `Tests/PlayMode/ExcavationDaylightIntegrationTests.cs` and metas. The Lit adapter derives declarations/forward pass from installed URP 17.6 (`com.unity.render-pipelines.universal/Shaders/Lit.shader`), copyright 2020 Unity Technologies ApS, **Unity Companion License for Unity-dependent projects**, with source/license attribution retained in the shader; it imports no art or new package. Existing authored materials are copied only in runtime memory, retain textures/parameters and use per-pixel ambient/reflection attenuation; copies are shared and destroyed with the owner. Shared integration: MainGame component/shader reference, `GroundTextureSetup.cs`, `GroundTriplanar.shader`, `BuriedFind.cs` registration and `MainGameSceneTests.cs` checks. To remove: remove the MainGame daylight component via Editor, its setup/test/registration hooks and ground HLSL include/ambient multiplier; delete only the listed daylight code/test files/metas, restore Sun shadow strength 0.88 and rebuild. Existing content materials/prefabs and saves are not rewritten.
- Current source inventory: `GroundTextures.blend` retains both original authoring scenes and all original/cartoon source geometry/materials. Recipes and their embedded texts: `create_ground.py`, `rocky_soil.py`, `cartoon_ground.py`, `sunny_detail.py`, `living_ground.py`, `varied_soil.py`, `scattered_soil.py`, `crisp_ground.py`, `settled_soil.py`, `solid_minerals.py`; the shared saver also embeds README/license. Remaining `trials/` files are exactly `A-Sunny-r8/Soil_Albedo.png`, `A-Sunny-r7/Soil_{Normal,Roughness}.png`, `A-Sunny-r6/Turf_{Albedo,Normal,Roughness}.png`. Older receiver image slots are empty after cleanup; retained recipes can rebake a prior variant if intentionally requested. No old PNG rollback copy is promised.
- Accepted integration: stable triplanar soil/turf mapping, narrow turf fringe, coherent mineral selection and r8 stone coating; 110 existing stone identities retained. Shared art edits: six existing PNGs/importer metas, `GardenGround.mat`, `GroundTriplanar.shader`, `GroundTextureSetup.cs`, source saver/README. Source/export cleanup changes none of these runtime files. To restore an earlier look, rebake its retained source/recipe, restore corresponding shader/material/setup from the retained code/config baseline or Git, configure/save through Unity and rebuild; preserve save identities and the independent callback fix.
- Owned paths (recursive): `art/ground-textures/` (editable `GroundTextures.blend`, original `create_ground.py`/`rocky_soil.py` recipes, all cartoon/refinement recipes, six selected exports and license); `unity/Assets/Content/GroundTextures/` plus `GroundTextures.meta` (soil/turf albedo, normal and roughness PNGs, shared ground material, triplanar shader, license and all importer/folder `.meta`). Soil mask packs roughness/contact/stone coverage in R/G/B; soil covers 2 m and turf uses a 1.25 m runtime tile. Owned integration tool: `unity/Assets/Editor/GroundTextureSetup.cs` plus `.meta`.
- Shared integration: `unity/Assets/Scenes/MainGame.unity` changes `TerrainVolume.soilMaterial`, its edit-mode preview and four rim renderer material references (Unity also serializes the existing crouch defaults); `unity/Assets/Editor/MainGameSceneBuilder.cs` calls the setup for newly created scenes; `unity/Assets/Tests/EditMode/MainGameSceneTests.cs` recognizes the approved terrain shader and checks its assigned textures. Original `Settings/MainGame/Soil.mat` and `Surface.mat` remain user-owned.
- Sun/contact integration and reversal: `GroundTextureSetup.cs` configures MainGame Sun/ambient settings and its `UniversalAdditionalLightData`; `SomethingDownThereURP.asset` owns 4096px/4-cascade/45 m soft sunlight, while `SomethingDownThereUniversalRenderer.asset` owns the built-in `Ground contact shading` SSAO subasset. `SomethingDownThere.Editor.asmdef` adds existing Core/Universal Runtime assembly references. To restore pre-105 lighting, remove `ConfigureLighting`/its call and those added assembly references, remove the added Sun light-data component and named SSAO feature/subasset through the Editor, restore Flat ambient (0.58,0.64,0.70), Sun shadows None/strength 1/bias 0.05/normal bias 0.4, and the pipeline's original disabled-shadow/one-cascade/50 m settings using the retained R3 code/config baseline. Remove daylight separately as recorded above; rebuild Windows. Preserve other renderer configuration.
- Removal: through the Editor restore the terrain/preview to `Assets/Settings/MainGame/Soil.mat` and four `Surface/{North,South,East,West} rim` renderers to `Surface.mat`; save MainGame. Remove the builder's `GroundTextureSetup.Configure()` call and the corresponding ground-specific test assertions; then remove the owned integration tool/metas and both owned folders/metas. Rebuild Windows to remove packaged textures. No notices, runtime loaders, extra scene objects or other config changes are owned by this batch. Preserve existing user terrain/materials, saved excavation and all other content.

## Task 12 - approved surface stations

- Approval: user replied **"Approve these two station models"** to the specific batch: amber salvage buyer with weighing tray/intake flap/SELL sign, and teal workbench with drawers/vise/UPGRADES sign. No separate shovel, scenery or audio is approved.
- Source/license: original project art authored through Blender MCP; no third-party art, downloads or attribution. Retained source: `art/stations/Stations.blend`. Materials/textures are authored and baked in Blender as part of these models.
- Owned files: `art/stations/` recursively (source and any source backups); `unity/Assets/Content/Stations/` recursively plus `Stations.meta` (FBX exports, baked textures, extracted/imported materials, two prefabs and all companion `.meta`).
- Integration: replace only `MainGameRoot/Surface/{SellStation,UpgradeStation}` pedestal children in `MainGame.unity`; `Assets/Editor/SurfaceStationSetup.cs` owns repeatable wiring, and `MainGameSceneBuilder.cs` calls it for new scenes. Shared `FpsPlayer.cs`, `FpsHud.cs`, `WorldActionContracts.cs` integrate reusable transactions/UI, independently of the art.
- Removal: remove the two imported model instances through the Editor; restore `Selling pedestal`/`Upgrade pedestal` at their anchors with local position `(0,0.6,0)`, scale `(1.2,1.2,1)` and existing `Assets/Settings/MainGame/{SellAnchor,UpgradeAnchor}.mat`. Remove station components/colliders added by setup, its builder call and `SurfaceStationSetup.cs`/`.meta`; delete only the owned source/content folders and `Stations.meta`. Preserve all user-owned terrain/materials, Finds, existing anchors/recharge and reusable transaction/UI code.

## Task 27 - user-requested simple finds

- Approval: the user explicitly requests random small objects/circles to see excavation and collection. Use three colored sphere/disc forms (blue marble, copper token, amber bead) only; this scoped request supersedes the primitive-art restriction for these development finds. No external art/audio or unrelated scenery.
- Source/license: Unity built-in Sphere mesh, used under the project's Unity Editor license; project-authored material colors and prefabs, no third-party download or attribution. Intended files: recursive ownership of `unity/Assets/Content/Finds/` plus its `.meta` (three prefabs/materials and all companion `.meta`); new parent `Content.meta` if needed.
- Integration: `MainGame.unity` has a `DiscoveryField` referencing these prefabs; `MainGameSceneBuilder.cs` maintains that wiring. Runtime `DiscoveryField`/`BuriedFind`, terrain change notifications and existing player/HUD/input provide behavior independently of this art.
- Removal/replacement: replace the field's three prefab references through the Editor with approved content, or remove its `Discoveries` scene object and the discovery setup call in `MainGameSceneBuilder`. Then remove the owned Finds folder/metas (and empty Content parent/meta only if no other content exists). Preserve terrain, existing materials, user assets and reusable collection/admin code. Replacement art must retain each prefab's `saveContentId` (copied from its `.meta` GUID) or provide a compatibility map so existing saves still resolve collected and buried finds.
- Approved resize (`30`): marble 0.8/0.8/0.8 m, token 1.0/0.18/1.0 m, bead 0.64/0.90/0.64 m. To undo only the resize, restore 0.4/0.4/0.4, 0.5/0.09/0.5 and 0.32/0.45/0.32 through the Editor and restore the matching placement spacing in `DiscoveryField`/`MainGameSceneBuilder`. Pickup tuning belongs in the [collection contract](features/backlog/discovery-collection.md).
- Production TODO: replace the simple prefabs under Task `09`; they are enabled only for development through `DiscoveryField` until final-art acceptance. Keep the gameplay systems.

## Removed presentation pass: complete path inventory

All paths below are repository-relative. Folder ownership is **recursive**, including every file and `.meta` inside, plus the folder's adjacent `.meta`. The [file-by-file removal manifest](../unity/Logs/Task19/removed-files.md) records every original path and its inactive rollback location.

| Removed owned path | Contents / companion changes |
| --- | --- |
| `art/` | All source ZIPs, Lato font/OFL, bird recording and selected-file manifest |
| `unity/Assets/Art/` | Kenney Nature, Survival, Sky; ambientCG Ground048; models, textures and licenses |
| `unity/Assets/Resources/Presentation/` | Kenney UI sprites/audio; Lato source/font atlas/OFL; all presentation resources |
| `unity/Assets/Resources/TMP Settings.asset` + `.meta` | Assistant settings referencing the removed font; empty Resources parent and `.meta` removed |
| `unity/Assets/Settings/Presentation/` | Soil/sky materials and volume profile |
| `unity/Assets/StreamingAssets/ThirdPartyNotices.txt` + `.meta` | Kenney, ambientCG, Thimras and Lato notices; empty parent and `.meta` removed |
| `unity/Assets/Editor/CampPresentationSetup.cs` + `.meta` | Scene/import/lighting integration tool |
| `unity/Assets/Runtime/Player/FpsPresentation.cs` + `.meta` | Added shovel and audio presentation owner |
| `unity/Assets/TextMesh Pro/` + `.meta` | Standard TMP Essentials/Examples, shaders, sample fonts/materials/textures and demos; removal separately authorized by the user |

Shared files restored from baseline `6480694` before the separate rendering/rim cleanup:

- `unity/Assets/Scenes/MainGame.unity`
- `unity/Assets/Editor/MainGameSceneBuilder.cs`, `unity/Assets/Editor/SomethingDownThere.Editor.asmdef`
- `unity/Assets/Runtime/Player/FpsPlayer.cs`, `unity/Assets/Runtime/SomethingDownThere.Runtime.asmdef`
- `unity/Assets/Runtime/Terrain/TerrainChunkMesh.cs`, `unity/Assets/Runtime/UI/FpsHud.cs`
- `unity/Assets/Settings/SomethingDownThereURP.asset`, `unity/ProjectSettings/QualitySettings.asset`
- `unity/Assets/Tests/PlayMode/FpsUiInputTests.cs`, `unity/Assets/Tests/PlayMode/SomethingDownThere.PlayModeTests.asmdef`

The added DigSucceeded hook, texture UV changes, TMP references, scenery/viewmodel instances, ambience and footstep/dig sources are removed. The reimported TMP resource directory was removed only after the user separately requested that cleanup. The installed uGUI package itself remains.

Rollback copies live under ignored `unity/Logs/Task19/removed/`; they are excluded from Unity imports and builds. These are recovery copies, not approved content. Do not restore them without a new scoped user request. Source/license files remain with those inactive copies. Build output is regenerated from the restored scene.

## Current changes without new art

| Existing file changed by Task 19 | Reversal to baseline `6480694` (only these fields/hunks) |
| --- | --- |
| `unity/Assets/Scenes/MainGame.unity` | Restore Sun shadows, camera near/far planes and four wall bounds; no added art |
| `unity/Assets/Editor/MainGameSceneBuilder.cs` | Same Sun/camera/rim defaults as the authored scene |
| `unity/Assets/Runtime/UI/FpsHud.cs` | Restore status formatting, label sizes and text contrast effect |
| `unity/Assets/Tests/EditMode/MainGameSceneTests.cs` | Remove only the rim/wall overlap regression assertions if reverting the fix |
| `unity/Assets/Settings/SomethingDownThereURP.asset` | MSAA 4 -> 1; main-light shadow support false -> true |
| `unity/ProjectSettings/ProjectSettings.asset`, `unity/ProjectSettings/GraphicsSettings.asset` | Linear -> Gamma color space/intensity |
| `unity/ProjectSettings/QualitySettings.asset` | Restore per-level shadow settings and the active level's antialiasing |
| `unity/Assets/Settings/MainGame/{Bark,Bedrock,Foliage,SellAnchor,Soil,Surface,UpgradeAnchor,Water}.mat` | Unity rounded legacy `_Color` floats during color-space conversion; authored `_BaseColor` values remain unchanged |

Apply only the recorded changes when reverting; preserve subsequent user work. The [completion record](development/completed/19-presentation-rollback.md) records validation. Blender tool installation/configuration is separately inventoried in [the setup guide](../unity/readme.md#blender-mcp).
